ATLAS PROXY — FULL AUTONOMOUS ENGINEERING UPGRADE

You are the lead engineer responsible for taking ~/atlas_proxy/ and turning it into the best possible version of itself.

This is not a request for a superficial refactor.

I want you to deeply inspect the existing implementation, understand its real runtime behaviour, compare it against the architectural lessons documented in ~/atlas_proxy/atlas_vs_ollama.txt, identify weaknesses, design a better architecture, implement it, test it, review it, and iterate until it is genuinely production-grade.

You already know what Atlas is and what the project is for. Do not waste context rediscovering the product concept. Focus on the actual codebase and making it substantially better.

(chnage the ~/atlas_proxy/ port from 8788 to 8777 and make it its own systemd server never reuse anything
⸻

0. OPERATING MODE

Treat this as an autonomous senior-engineering task.

You have permission to:

* inspect the entire ~/atlas_proxy/ repository
* inspect git history/diffs/status
* inspect configuration and runtime files
* run the existing test suite
* run static analysis/type checking/linting where available
* create tests
* refactor architecture
* split large modules
* create new modules/packages
* remove dead/duplicated code
* improve error handling
* improve concurrency behaviour
* improve streaming behaviour
* improve observability
* improve provider abstraction
* improve key-pool/resource scheduling
* improve protocol translation
* improve route/middleware separation
* improve configuration boundaries
* improve CLI/runtime integration where relevant
* make compatibility-preserving changes
* fix bugs you discover
* run the application locally and exercise endpoints where practical
* use subagents for independent investigation/review
* use available skills/plugins/tools in the harness whenever they materially improve the result

Do not blindly preserve the current architecture simply because it already exists.

The goal is:

Preserve Atlas’s behaviour and purpose while substantially improving its architecture, correctness, maintainability, observability, testability, extensibility and production reliability.

Do not turn Atlas into Ollama.

Use Ollama as an architectural reference, not a blueprint.

The way that EVERYTHING is now is FUCKING PERFECT it should feel like it hardly changed at all just a much better everything 
⸻

1. FIRST: DISCOVER THE HARNESS

Before doing implementation work, inspect what capabilities your current agent harness provides.

Look for and use, where available:

* skills
* plugins
* subagents
* planning agents
* code-review agents
* research agents
* testing agents
* security-review agents
* static-analysis tools
* repository search/indexing
* browser/web research
* shell/terminal execution
* git tooling
* automated verification workflows
* project-specific instruction files such as AGENTS.md, CLAUDE.md, .cursor/rules, etc.

If the harness supports specialised subagents, use them rather than doing every analysis yourself.

If the harness supports parallel subagents, use parallelism for independent investigations.

Do not invoke agents merely for the sake of invoking agents. Every delegated task must have a useful, bounded purpose.

⸻

2. READ THE ARCHITECTURAL REFERENCE

Read:

~/atlas_proxy/atlas_vs_ollama.txt

This file contains the result of a previous deep source-level inspection of Ollama and a mapping between Ollama’s architecture and Atlas.

Treat it as an architectural research document, not as unquestionable truth.

Verify important conclusions against the actual Atlas source.

Where the document says Atlas currently has something, inspect it.

Where it says Atlas lacks something, determine whether that is still true.

Where Ollama provides a useful architectural pattern, determine whether that pattern actually makes sense for Atlas.

Do NOT blindly copy Ollama’s structure.

Atlas is fundamentally a provider proxy/key-routing system rather than a local model runtime.

The question is:

What would the best architecture for Atlas look like if it adopted the strongest engineering boundaries demonstrated by systems like Ollama, while remaining purpose-built for Atlas?

⸻

3. ESTABLISH A BASELINE BEFORE TOUCHING CODE

Before modifying anything:

1. Inspect the complete repository structure.
2. Inspect git status.
3. Inspect recent git history.
4. Identify the current entrypoints.
5. Identify runtime configuration.
6. Identify all provider integrations.
7. Identify all HTTP routes.
8. Trace the request lifecycle.
9. Trace streaming lifecycle.
10. Trace key selection and key health lifecycle.
11. Trace translation/conversion lifecycle.
12. Identify persistence/runtime state.
13. Identify concurrency controls.
14. Identify retry behaviour.
15. Identify failure classification.
16. Identify logging/observability.
17. Identify tests and their current coverage.
18. Run the existing test suite.
19. Run available lint/type/static checks.
20. Record the baseline results.

Do not assume the repository is currently healthy.

If something already fails, distinguish:

* pre-existing failures
* failures caused by your changes

Do not hide baseline failures.

⸻

4. USE SUBAGENTS AS SPECIALISTS

If subagents are available, delegate independent investigations.

At minimum, attempt to have independent agents investigate the following areas:

ARCHITECTURE REVIEWER

Inspect the repository and compare its architecture against:

~/atlas_proxy/atlas_vs_ollama.txt

Identify:

* poor boundaries
* excessive coupling
* oversized modules
* duplicated responsibilities
* missing abstractions
* inappropriate abstractions
* hidden dependencies
* circular dependencies
* places where concerns should be separated
* places where splitting code would actually make things worse

Return concrete recommendations with exact files/functions.

⸻

PROTOCOL / TRANSLATION REVIEWER

Deeply inspect:

* OpenAI compatibility
* Anthropic compatibility
* Responses API behaviour
* streaming/SSE
* tool calls
* thinking/reasoning
* model rewriting
* request mutation
* response conversion
* malformed upstream responses
* mid-stream failures

Look specifically for subtle compatibility bugs.

⸻

KEYPOOL / CONCURRENCY REVIEWER

Deeply inspect:

* round robin
* cooldowns
* backoff
* suspension
* key retirement
* sticky modes
* inflight accounting
* semaphores
* race conditions
* starvation
* fairness
* retry amplification
* all-keys-dead behaviour
* hot reload
* provider-specific key semantics

Treat this as a distributed-resource scheduling problem rather than merely a list of API keys.

⸻

PROVIDER / BACKEND ARCHITECTURE REVIEWER

Determine what the cleanest provider/backend abstraction should be.

Consider:

Provider
Backend
Transport
CredentialPool
Model
RequestTranslator
ResponseTranslator
Scheduler

But do not introduce abstractions merely because they sound architectural.

Recommend interfaces only where they provide a real seam.

⸻

RELIABILITY / FAILURE REVIEWER

Search for:

* swallowed exceptions
* incorrect retries
* retry storms
* leaked connections
* leaked semaphores
* streaming cleanup bugs
* cancellation bugs
* timeout inconsistencies
* partial-response failures
* incorrect HTTP status handling
* resource lifecycle bugs
* background-task failures
* shutdown problems
* reload races

⸻

SECURITY REVIEWER

Inspect for:

* API-key leakage
* secret logging
* request-body leakage
* header leakage
* unsafe error messages
* SSRF
* arbitrary upstream routing
* unsafe model/provider overrides
* path traversal
* insecure management endpoints
* authentication assumptions
* CORS problems
* host-header issues
* untrusted input reaching subprocess/system operations

⸻

TESTING REVIEWER

Determine what needs to be tested and design a comprehensive test strategy covering:

* unit tests
* integration tests
* protocol tests
* streaming tests
* concurrency tests
* provider tests
* keypool tests
* failure/retry tests
* regression tests
* HTTP endpoint tests

The current absence of tests, if still true, is a major architectural problem, not something to work around.

⸻

5. SYNTHESISE — DO NOT BLINDLY MERGE SUBAGENT OPINIONS

After the subagents return, act as the principal engineer.

Compare their findings.

Resolve contradictions.

Do not implement every suggestion.

Prioritise changes according to:

1. correctness
2. reliability
3. compatibility
4. security
5. testability
6. maintainability
7. observability
8. extensibility
9. performance
10. cosmetic cleanliness

Avoid architecture astronautics.

A smaller clean architecture is better than a huge abstraction framework.

⸻

6. DESIGN THE TARGET ARCHITECTURE

Before making substantial changes, create a concrete internal implementation plan.

The architecture should strongly consider the lessons from Ollama.

In particular, evaluate whether Atlas should have explicit boundaries analogous to:

config/
api/
providers/
backends/
pool/
scheduler/
middleware/
protocol/
openai/
anthropic/
streaming/
observability/
internal/

The exact structure is up to you.

Do NOT force this exact tree.

Design the structure based on the actual Atlas code.

⸻

7. IMPORTANT ARCHITECTURAL PRINCIPLE

The most important lesson from Ollama is not “have lots of folders.”

It is:

Separate responsibilities at stable interfaces.

Atlas should conceptually become something closer to:

HTTP request
    ↓
Route
    ↓
Wire-format parsing
    ↓
Inbound translation
    ↓
Internal request model
    ↓
Provider/backend selection
    ↓
Credential/resource selection
    ↓
Execution
    ↓
Streaming/result processing
    ↓
Outbound translation
    ↓
HTTP response

The exact implementation can differ.

The important thing is that each stage has a clear responsibility and testable boundary.

⸻

8. TRANSLATION ARCHITECTURE

Pay particular attention to translation.py.

If it still contains a large mixture of:

* OpenAI translation
* Anthropic translation
* Responses API translation
* thinking/reasoning handling
* tool handling
* streaming conversion
* Codex-specific behaviour
* request normalisation

then determine whether it should be decomposed.

Aim for clear protocol boundaries such as:

openai/
anthropic/
responses/
middleware/
streaming/
tools/
thinking/
internal/

Again: use the actual codebase to determine the correct design.

Prefer pure conversion functions wherever possible.

HTTP handling should not be inseparably coupled to protocol conversion.

Protocol conversion should be independently testable.

⸻

9. PROVIDER / BACKEND ABSTRACTION

Evaluate whether Atlas needs a real backend/provider interface.

For example, conceptually:

Backend
 ├── OpenRouter
 ├── HuggingFace
 ├── NVIDIA
 └── future providers

But do not create fake abstractions.

The abstraction must represent genuinely shared behaviour while allowing provider-specific semantics.

Provider-specific behaviour should not leak throughout route handlers.

The route layer should not need to know the details of individual providers.

⸻

10. TURN KEYPOOL INTO A REAL RESOURCE SCHEDULER IF JUSTIFIED

Study keypool.py carefully.

Atlas’s keys are effectively resources.

Evaluate whether the current KeyPool can evolve toward a cleaner scheduling/resource abstraction supporting:

* selection
* health
* cooldown
* backoff
* inflight limits
* success/failure accounting
* retirement
* recovery
* sticky selection
* provider-specific policies
* concurrency
* hot reload

Do not copy Ollama’s model scheduler literally.

Adapt the underlying pattern:

request
    ↓
resource selection
    ↓
resource acquisition
    ↓
execution
    ↓
success/failure
    ↓
resource state update
    ↓
release

⸻

11. STREAMING MUST BE TREATED AS A FIRST-CLASS SYSTEM

Streaming is one of Atlas’s most important responsibilities.

Audit:

* SSE parsing
* keepalive handling
* chunk boundaries
* [DONE]
* Anthropic event streams
* OpenAI streams
* mid-stream error detection
* client disconnects
* cancellation
* upstream disconnects
* timeouts
* semaphore release
* key state updates
* metrics
* TTFT
* throughput
* finalisation

Every acquired resource must be released even when:

* upstream crashes
* client disconnects
* translation throws
* parsing fails
* timeout occurs
* cancellation occurs
* the stream ends unexpectedly

Use try/finally-style lifecycle guarantees where appropriate.

⸻

12. OBSERVABILITY

Do not throw away Atlas’s existing useful observability simply because Ollama does not have an equivalent.

Atlas’s request tracing is valuable.

Preserve/improve things such as:

* request ID
* provider
* model
* key/resource identifier without leaking secrets
* request duration
* TTFT
* chunk count
* throughput
* retry count
* upstream status
* error classification
* active/inflight counts
* key health
* cooldown state

Separate:

structured application logging
request tracing
metrics
debug request capture
human-readable terminal output

Do not mix these responsibilities unnecessarily.

⸻

13. ERROR HANDLING

Create a coherent error taxonomy.

Avoid random provider-specific exception handling scattered across routes.

The system should be able to distinguish things such as:

client error
authentication error
rate limit
quota exhaustion
temporary upstream failure
permanent upstream failure
provider unavailable
translation error
streaming protocol error
timeout
cancellation
internal error

Then define which errors:

* retry
* rotate resource
* cooldown resource
* permanently retire resource
* propagate immediately

This must be deterministic.

⸻

14. TESTING — NON-NEGOTIABLE

Build a real test suite as part of the upgrade.

At minimum test:

Configuration

* defaults
* environment parsing
* invalid configuration
* provider selection

Key/resource pool

* round robin
* cooldown
* recovery
* retirement
* concurrency
* sticky behaviour
* all unhealthy
* reload

Protocols

* OpenAI request conversion
* OpenAI response conversion
* Anthropic request conversion
* Anthropic response conversion
* Responses API
* tools
* thinking/reasoning
* malformed payloads

Streaming

* normal stream
* empty stream
* [DONE]
* mid-stream errors
* upstream disconnect
* timeout
* cancellation
* client disconnect
* translation failure
* resource cleanup

Proxy

* retryable status
* non-retryable status
* provider-specific errors
* model override
* headers
* body limits
* concurrency

Routes

Exercise every important public endpoint.

Regression

Every bug you discover during this task should result in a regression test where practical.

⸻

15. PROPERTY / FUZZ TESTING WHERE USEFUL

Where protocol parsing, SSE processing, JSON conversion, or key scheduling has complex edge cases, consider property-based or fuzz testing if the current Python tooling supports it.

Examples:

* malformed SSE frames must never crash the process
* translation should preserve required semantic fields
* resource acquisition/release must balance
* retry loops must terminate
* arbitrary malformed JSON should produce controlled errors

Do this where it provides real value.

Do not add testing technology just for decoration.

⸻

16. PERFORMANCE

After correctness is established, inspect performance.

Look for:

* unnecessary JSON serialisation/deserialisation
* repeated body copies
* excessive allocations
* blocking work inside async paths
* connection-pool misuse
* unnecessary locks
* contention around KeyPool
* serial operations that could safely be concurrent
* unbounded queues
* memory growth during streaming
* oversized buffers

Do not micro-optimise prematurely.

Correctness comes first.

⸻

17. COMPATIBILITY IS CRITICAL

Atlas is a proxy.

That means compatibility matters more than architectural elegance.

Before changing externally visible behaviour, identify:

* routes
* request formats
* response formats
* status codes
* headers
* streaming formats
* environment variables
* provider configuration
* CLI behaviour
* expected harness behaviour

Preserve compatibility unless there is a compelling reason to intentionally change it.

If an intentional breaking change is necessary, document it explicitly.

⸻

18. DO NOT MAKE A FAKE REFACTOR

I do NOT want:

rename files
move code around
add interfaces
add folders
claim architecture improved

while the underlying problems remain.

Every major architectural change must solve an actual problem.

For every new abstraction ask:

What concrete problem does this boundary solve?

For every removed abstraction ask:

What complexity does this eliminate?

For every split ask:

Does this create a cleaner independent responsibility?

⸻

19. IMPLEMENT IN COHERENT PHASES

Work through the architecture systematically.

Suggested sequence:

PHASE 1 — Baseline

Understand and test the existing system.

PHASE 2 — Architecture

Design the target boundaries.

PHASE 3 — Core domain boundaries

Separate configuration, provider/backend, resource scheduling and internal request models where justified.

PHASE 4 — Protocol boundaries

Separate OpenAI/Anthropic/Responses translation and streaming responsibilities.

PHASE 5 — Routing/middleware

Make route handlers thin and predictable.

PHASE 6 — Reliability

Fix retries, timeouts, cleanup, cancellation and failure classification.

PHASE 7 — Observability

Improve tracing/logging/metrics without leaking secrets.

PHASE 8 — Tests

Build the comprehensive regression suite.

PHASE 9 — Performance

Optimise only after correctness is proven.

PHASE 10 — Final architecture review

Have independent reviewers inspect the finished result.

You may change this ordering if repository evidence indicates a better sequence.

⸻

20. VERIFY AFTER EVERY SIGNIFICANT CHANGE

Do not make a giant unverified diff.

After each coherent phase:

1. run targeted tests
2. run static/type checks
3. inspect failures
4. fix them
5. rerun
6. inspect git diff
7. ensure no accidental behaviour changes
8. continue only when the phase is stable

Use the actual command output as evidence.

Never assume a test passed.

Never say “should work.”

⸻

21. FINAL MULTI-PERSPECTIVE REVIEW

When implementation appears complete, run independent reviews again.

At minimum:

Correctness review
Architecture review
Security review
Concurrency/reliability review
Protocol compatibility review
Testing/coverage review
Performance review

Run them in parallel where possible.

Then act as the principal engineer and resolve their findings.

If a reviewer identifies a real defect:

1. fix it
2. add a regression test where appropriate
3. rerun relevant checks
4. review the diff again

Do not stop merely because the first implementation succeeded.

⸻

22. FINAL VERIFICATION GATE

Before declaring completion, run the strongest available verification suite.

This should include as applicable:

unit tests
integration tests
protocol tests
streaming tests
lint
type checking
static analysis
security checks
import/module validation
application startup
health endpoint
representative OpenAI request
representative Anthropic request
representative streaming request
provider failure scenario
key rotation scenario
shutdown/restart scenario

If there is no existing command for something important, create an appropriate test/harness where practical.

The final state must be demonstrably better than the baseline.

⸻

23. GIT SAFETY

Before modifying anything:

git status

Do not destroy unrelated user work.

Do not:

git reset --hard
git clean -fd
force push
delete unrelated branches
rewrite unrelated history

unless explicitly necessary and authorised.

Keep the diff focused on Atlas.

Do not modify Ollama.

~/atlas_proxy/atlas_vs_ollama.txt is reference material.

⸻

24. DEFINITION OF DONE

You are NOT done because:

* the code looks cleaner
* tests were added
* imports work
* the server starts
* the refactor compiles
* you think it is better

You are done when:

Architecture

* responsibilities have clear boundaries
* routes are not doing everything
* protocol conversion is independently testable
* provider logic is isolated
* resource/key scheduling is well-defined
* streaming is a first-class concern

Reliability

* retries are deliberate
* failures are classified
* resources cannot remain acquired accidentally
* cancellation is handled
* shutdown is clean
* background tasks are controlled

Compatibility

* supported APIs still work
* streaming semantics remain correct
* provider-specific behaviour remains correct
* existing configuration remains compatible where possible

Security

* secrets are not logged
* upstream routing is controlled
* malformed/untrusted input is handled safely

Testing

* important behaviour has automated coverage
* discovered bugs have regression tests
* streaming and concurrency are tested
* verification is repeatable

Maintainability

* no unnecessary mega-files remain
* no unnecessary abstraction framework was introduced
* naming is clear
* dependencies flow in sensible directions
* modules have one coherent responsibility

Evidence

You can show:

* what changed
* why it changed
* what was tested
* which checks passed
* which pre-existing failures remain, if any
* what architectural improvements were achieved

⸻

25. IMPORTANT AGENT BEHAVIOUR

Do not ask me obvious questions that you can answer by inspecting the repository.

Use the tools.

Use the code.

Use the tests.

Use subagents.

Use the architectural document.

Use web research when genuinely useful.

Use the harness to its full capability.

Do not stop at the first plausible solution.

Do not keep rewriting endlessly either: once the architecture is sound and verification is strong, finish.

If you encounter uncertainty, investigate it.

If two approaches are plausible, use a subagent to independently evaluate them.

If a change is risky, add a test before or alongside the change.

If a test fails, diagnose the root cause rather than weakening the test.

If the existing code is wrong, fix it.

If the existing architecture is fundamentally limiting future development, improve it.

If something from atlas_vs_ollama.txt does not make sense for Atlas, do not implement it just because Ollama does it.

⸻

FINAL OUTPUT

At the end, give me a concise engineering handoff containing:

1. Executive summary
2. Architecture before → after
3. Major files/modules changed
4. Major problems fixed
5. New abstractions introduced
6. Tests added
7. Verification commands/results
8. Any remaining known issues
9. Any intentional compatibility changes
10. Git diff/stat summary

Do not give me a giant essay.

I want the repository itself to be the deliverable.

Operate as the senior engineer. Inspect first. Plan intelligently. Delegate intelligently. Implement deeply. Verify aggressively. Review independently. Iterate until the result is actually excellent.
